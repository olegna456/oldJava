package last;
import java.util.*;
class Main {
	
	public static void main(String[] args) {
		SetDimension s = new SetDimension();
		int row = s.inputRow();
		int column = s.inputColumn();
		SetDimension diSet = new SetDimension(new Set(), row, column);
		
		
	}
}